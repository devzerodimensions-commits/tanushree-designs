{
  "name": "tanushree-designs",
  "version": "1.0.0",
  "private": true,
  "description": "Website and admin panel for Tanushree Designs",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",

    "dev:db": "npm run db:dev --prefix server",
    "dev:api": "npm run dev --prefix server",
    "dev:web": "npm run dev --prefix client",

    "db:migrate": "npm run db:migrate --prefix server",
    "db:seed": "npm run db:seed --prefix server",
    "db:reset": "npm run db:reset --prefix server",

    "build": "npm run build --prefix client",

    "render-build": "npm ci --prefix server --omit=dev && npm ci --prefix client --include=dev && npm run build --prefix client",
    "start": "node server/src/index.js"
  }
}
